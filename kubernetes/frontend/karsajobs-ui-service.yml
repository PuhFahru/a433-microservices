apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui-service
spec:
  # Expose the Vue frontend so it can be opened from the host browser.
  type: NodePort
  selector:
    app: karsajobs-ui
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081
